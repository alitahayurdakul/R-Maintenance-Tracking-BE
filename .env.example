# Copy to server/.env and adjust. Nothing here is shared with the app's own
# .env - the frontend only ever sees this server through its API_URL.
PORT=5050
MONGODB_URI=mongodb://127.0.0.1:27017/tms-maintenance
ACCESS_TOKEN_SECRET=change-me-access
REFRESH_TOKEN_SECRET=change-me-refresh
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
# Origins allowed to send credentialed requests, comma-separated.
CLIENT_ORIGIN=http://localhost:3000

# Set both when the frontend is on a different site than this API (a deployed
# API with a local frontend, say): a refresh cookie is dropped by the browser
# across sites unless it is SameSite=None and Secure.
# COOKIE_SAMESITE=none
# COOKIE_SECURE=true
